const initialState = {
    
}

function authReducer(state = initialState, action) {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}