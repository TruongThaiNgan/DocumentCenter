const initialState = {
    test: 1
}

function authReducer(state = initialState, action) {
    // switch (action) {
    //     case value:
            
    //         break;
    
    //     default:
    //         break;
    // }
    return state;
}

export default authReducer;